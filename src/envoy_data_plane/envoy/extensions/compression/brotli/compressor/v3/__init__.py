# Generated by the protocol buffer compiler.  DO NOT EDIT!
# sources: envoy/extensions/compression/brotli/compressor/v3/brotli.proto
# plugin: python-betterproto
from dataclasses import dataclass
from typing import Optional

import betterproto
from betterproto.grpc.grpclib_server import ServiceBase


class BrotliEncoderMode(betterproto.Enum):
    DEFAULT = 0
    GENERIC = 1
    TEXT = 2
    FONT = 3


@dataclass(eq=False, repr=False)
class Brotli(betterproto.Message):
    """[#next-free-field: 7]"""

    # Value from 0 to 11 that controls the main compression speed-density lever.
    # The higher quality, the slower compression. The default value is 3.
    quality: Optional[int] = betterproto.message_field(1, wraps=betterproto.TYPE_UINT32)
    # A value used to tune encoder for specific input. For more information about
    # modes, please refer to brotli manual: https://brotli.org/encode.html#aa6f
    # This field will be set to "DEFAULT" if not specified.
    encoder_mode: "BrotliEncoderMode" = betterproto.enum_field(2)
    # Value from 10 to 24 that represents the base two logarithmic of the
    # compressor's window size. Larger window results in better compression at
    # the expense of memory usage. The default is 18. For more details about this
    # parameter, please refer to brotli manual:
    # https://brotli.org/encode.html#a9a8
    window_bits: Optional[int] = betterproto.message_field(
        3, wraps=betterproto.TYPE_UINT32
    )
    # Value from 16 to 24 that represents the base two logarithmic of the
    # compressor's input block size. Larger input block results in better
    # compression at the expense of memory usage. The default is 24. For more
    # details about this parameter, please refer to brotli manual:
    # https://brotli.org/encode.html#a9a8
    input_block_bits: Optional[int] = betterproto.message_field(
        4, wraps=betterproto.TYPE_UINT32
    )
    # Value for compressor's next output buffer. If not set, defaults to 4096.
    chunk_size: Optional[int] = betterproto.message_field(
        5, wraps=betterproto.TYPE_UINT32
    )
    # If true, disables "literal context modeling" format feature. This flag is a
    # "decoding-speed vs compression ratio" trade-off.
    disable_literal_context_modeling: bool = betterproto.bool_field(6)
